Для того чтобы соединить два запроса, они должны быть одинаков по структуру, допуустим если запрашиваются
производители компьютеров, но чтобы сделать из двух запросов один, необходимо выполнить UNION:

### SELECT maker FROM PC
### UNION
### SLECET maker FROM Laptop

Для того чтобы найти пересечение или разность по кругам Эйлера существуют два оператора INTERSECT и EXCEPT
Как и для объединения, выводимые поля должны совпадать в запросах:

### SELECT maker FROM PC
### INTERSECT
### SELECT maker FROM Laptop
, выведет данные которые есть и в PC, и в Laptop

### SELECT maker FROM PC
### EXCEPT
### SELECT maker FROM Laptop
, выведет все данные, которые есть в PC, но нет в Laptop
